# MySQL 连接配置位（待用户填入实际连接信息）
# 当 jdbc.url 为空时，集成测试自动使用 H2 内存数据库。
# 切换到 MySQL 时填入以下三项，重跑测试即可。

jdbc.url=
jdbc.user=
jdbc.password=

# 示例（取消注释并改成实际值）：
# jdbc.url=jdbc:mysql://127.0.0.1:3306/intentio_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
# jdbc.user=root
# jdbc.password=secret
